# Python prototype (archived)

Original Dear PyGui prototype. **Not maintained.**

Active app is the Rust version at the repo root (`cargo run`).

```powershell
cd archive/python
pip install -r requirements.txt
python main.py
```